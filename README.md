# SI360.GateRunner
Desktop application that runs every Pre-Deployment Gate in `SI360.Tests/PreDeploymentGate/`, aggregates results, computes `SatisfactionScorecard`, and produces a GO / HOLD / NO-GO deployment decision with full failure inventory.
